var notice = {
    init: function () {

    },
    findNoticeInfoList: function (page) {
        var state = $('#status').val();
        var title = $('#title').val();
    },
    deleteNotice: function (id) {
        if (!id) return;
    }
};